<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Politica de rede do Caixa Combo:
        - HTTPS obrigatorio em producao
        - HTTP permitido em debug apenas para 10.0.2.2 (loopback do emulador) e localhost
        - Supabase: HTTPS (production)
        - Vercel: HTTPS
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Loopback apenas para desenvolvimento/emulador -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.1.8</domain>
        <domain includeSubdomains="true">192.168.42.185</domain>
    </domain-config>
</network-security-config>
