<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Permissões para impressora SUNMI -->
    <uses-permission android:name="com.sunmi.permission.PRINTER" />

    <!-- Permissão para selecionar imagens da galeria -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Permissões para ler e escrever arquivos (exportar/importar produtos) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />


    <application
        android:name=".CaixaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CaixaCombo"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CaixaCombo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity fullscreen para bloqueio de tela (substitui SYSTEM_ALERT_WINDOW) -->
        <activity
            android:name=".LockActivity"
            android:exported="false"
            android:theme="@style/Theme.CaixaCombo.Lock"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true" />

        <!-- Polling Service REST para comunicação com Dashboard (Stone compliance - sem WebSocket) -->
        <service
            android:name=".service.PollingService"
            android:enabled="true"
            android:exported="false" />

        <!-- Device Admin Receiver para modo quiosque -->
        <receiver
            android:name="com.seucaixa.caixacombo.AdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Receiver para desprovisionar Device Owner via ADB -->
        <!-- Uso: adb shell am broadcast -a com.seucaixa.caixacombo.UNPROVISION -->
        <receiver
            android:name=".UnprovisionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.seucaixa.caixacombo.UNPROVISION" />
            </intent-filter>
        </receiver>
        
    </application>

</manifest>
